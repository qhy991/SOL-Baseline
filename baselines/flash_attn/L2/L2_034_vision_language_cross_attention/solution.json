{
  "name": "flash_attn_034_vision_language_cross_attention_baseline",
  "definition": "034_vision_language_cross_attention_fusion",
  "author": "flash_attn",
  "description": "FlashAttention baseline for L2_034 vision-language cross attention",
  "spec": {
    "languages": ["pytorch"],
    "target_hardware": ["LOCAL"],
    "entry_point": "baseline.py::run",
    "destination_passing_style": false
  },
  "sources": [
    {
      "path": "baseline.py",
      "content": "import torch\nimport torch.nn.functional as F\nimport math\nfrom flash_attn import flash_attn_func\n\n\n@torch.no_grad()\ndef run(\n    language_hidden_states, vision_hidden_states,\n    language_position_ids, vision_grid_thw,\n    q_proj_weight, q_proj_bias,\n    k_proj_weight, k_proj_bias,\n    v_proj_weight, v_proj_bias,\n    o_proj_weight,\n):\n    hidden_size = 4096\n    num_attention_heads = 32\n    num_key_value_heads = 8\n    head_dim = 128\n    rope_theta = 10000.0\n    dim_t, dim_h, dim_w = 42, 42, 44\n\n    batch_size, lang_seq_len, _ = language_hidden_states.shape\n    vision_seq_len = vision_hidden_states.shape[1]\n    device = language_hidden_states.device\n\n    # RoPE inverse frequencies\n    inv_freq_1d = 1.0 / (rope_theta ** (torch.arange(0, head_dim, 2, dtype=torch.float32, device=device) / head_dim))\n    inv_freq_t = 1.0 / (rope_theta ** (torch.arange(0, dim_t, 2, dtype=torch.float32, device=device) / dim_t))\n    inv_freq_h = 1.0 / (rope_theta ** (torch.arange(0, dim_h, 2, dtype=torch.float32, device=device) / dim_h))\n    inv_freq_w = 1.0 / (rope_theta ** (torch.arange(0, dim_w, 2, dtype=torch.float32, device=device) / dim_w))\n\n    def rotate_half(x):\n        x1 = x[..., : x.shape[-1] // 2]\n        x2 = x[..., x.shape[-1] // 2 :]\n        return torch.cat([-x2, x1], dim=-1)\n\n    def apply_rope_1d(q, position_ids, inv_freq):\n        position_ids_expanded = position_ids.unsqueeze(1).unsqueeze(-1).float()\n        freqs = position_ids_expanded * inv_freq\n        emb = torch.cat([freqs, freqs], dim=-1)\n        cos = emb.cos().to(q.dtype)\n        sin = emb.sin().to(q.dtype)\n        return (q * cos) + (rotate_half(q) * sin)\n\n    # Q projection (language)\n    query_states = F.linear(language_hidden_states, q_proj_weight, q_proj_bias)\n    query_states = query_states.view(batch_size, lang_seq_len, num_attention_heads, head_dim).transpose(1, 2)\n\n    # K, V projection (vision)\n    key_states = F.linear(vision_hidden_states, k_proj_weight, k_proj_bias)\n    value_states = F.linear(vision_hidden_states, v_proj_weight, v_proj_bias)\n    key_states = key_states.view(batch_size, vision_seq_len, num_key_value_heads, head_dim).transpose(1, 2)\n    value_states = value_states.view(batch_size, vision_seq_len, num_key_value_heads, head_dim).transpose(1, 2)\n\n    # 1D RoPE for Q\n    query_states = apply_rope_1d(query_states, language_position_ids, inv_freq_1d)\n\n    # 3D RoPE for K (preserve reference implementation)\n    k_t = key_states[..., :dim_t]\n    k_h = key_states[..., dim_t:dim_t+dim_h]\n    k_w = key_states[..., dim_t+dim_h:]\n    t_pos = vision_grid_thw[..., 0]\n    h_pos = vision_grid_thw[..., 1]\n    w_pos = vision_grid_thw[..., 2]\n    k_t = apply_rope_1d(k_t, t_pos, inv_freq_t)\n    k_h = apply_rope_1d(k_h, h_pos, inv_freq_h)\n    k_w = apply_rope_1d(k_w, w_pos, inv_freq_w)\n    key_states = torch.cat([k_t, k_h, k_w], dim=-1)\n\n    # FlashAttention format: [batch, seq_len, num_heads, head_dim]\n    query_states = query_states.transpose(1, 2).contiguous()\n    key_states = key_states.transpose(1, 2).contiguous()\n    value_states = value_states.transpose(1, 2).contiguous()\n\n    # FlashAttention cross-attention (non-causal, GQA-native)\n    attn_output = flash_attn_func(\n        query_states, key_states, value_states,\n        causal=False,\n        softmax_scale=1.0 / math.sqrt(head_dim),\n    )\n    attn_output = attn_output.reshape(batch_size, lang_seq_len, num_attention_heads * head_dim)\n\n    output = F.linear(attn_output, o_proj_weight)\n    return output\n"
    }
  ]
}